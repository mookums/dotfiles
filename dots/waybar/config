{
	"layer": "top",	
	"position": "bottom",
    "height": 22,

    "modules-left": ["hyprland/workspaces", "hyprland/window"],
    "modules-center": [],
    "modules-right": ["network", "battery", "clock", "tray"],


	"hyprland/workspaces": {
        "active-only": false,
        "format": "{id}" 
    },

	"hyprland/window": {
    	"format": "{}",
    	"max-length": 150
    },
    
    "battery": {
        "states": {
              "warning": 30,
              "critical": 15
    },

    "clock": {
        "format": "{:%a %b %e %R}",
        "interval": 30
    },

    "tray": {
        "icon-size": 18,
        "spacing": 15
    },

        "format": "{icon} {capacity}%",
        "format-icons": ["", "", "", "", ""],
        "interval": 30,
        "max-length": 25
    },

    "network": {
        "format": "{ifname}",
        "format-wifi": "W: {ipaddr}/{cidr} ({bandwidthTotalBits} bit/s)",
        "format-ethernet": "E: {ipaddr}/{cidr} ({bandwidthTotalBits} bit/s)",
        "format-disconnected": "down",
        "interval": 60,
        "max-length": 50
    }
}
